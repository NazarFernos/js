function getMaxNumber(arr) {
    var arr = [1, 0, 6, -80];
    var max = arr.length, iterationl;
    for(var i = arr.length; i >= 0; i--){
        iterationl = arr[i];
        if(iterationl > max){
            max = iterationl;
        }
    }
    return max;
}
console.log(getMaxNumber([]));
